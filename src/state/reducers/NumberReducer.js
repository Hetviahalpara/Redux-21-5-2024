const number_reducers=(state={number_name:'number',number_value:1},action)=>{
    if(action.type=='get'){
        return state
    }
    else if(action.type){
        return state
    }
    else{
        return state
    }
}

export default number_reducers